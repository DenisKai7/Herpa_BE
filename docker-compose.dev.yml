services:
  medical_backend:
    volumes:
      - ./app:/app/app
      - ./data:/app/data
      - ./.env:/app/.env
    command:
      - uvicorn
      - app.main:app
      - --host
      - 0.0.0.0
      - --port
      - "8000"
      - --reload
