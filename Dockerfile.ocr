# syntax=docker/dockerfile:1.7

FROM medical-ocr-base:torch-cpu-v1 AS runtime

ENV HF_HOME=/models/huggingface \
    TRANSFORMERS_CACHE=/models/huggingface

WORKDIR /worker

COPY app /worker/app

EXPOSE 8010

CMD ["uvicorn", "app.ocr_worker.main:app", "--host", "0.0.0.0", "--port", "8010"]
